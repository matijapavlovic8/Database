package hr.fer.oprpp1.db;

/**
 * Enumerates the possible {@link QueryToken} types.
 *
 * @author MatijaPav
 */
public enum QueryTokenType {
    EOF, JMBAG, LAST_NAME, FIRST_NAME, OPERATOR, STRING, AND
}
